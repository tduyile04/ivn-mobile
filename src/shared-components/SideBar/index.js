import React from 'react';
import { View, Text, Content, Container } from 'native-base';
import { StyleSheet } from 'react-native';

const SideBar = () => {
  return (
    <View style={{backgroundColor: '#fff'}}>
      <Text>Hello SideNav</Text>
    </View>
  );
};

export default SideBar;
